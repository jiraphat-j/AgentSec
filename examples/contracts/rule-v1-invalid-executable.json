{
  "schema_version": "1.0",
  "rule_id": "INVALID-EXECUTION",
  "rule_version": 1,
  "description": "Rejected because executable callbacks are outside the rule contract",
  "severity": "critical",
  "supported_event_versions": ["0.2"],
  "kind": "single_event",
  "steps": [
    {
      "name": "invalid",
      "predicates": [
        {"field": "event_type", "operator": "equals", "value": "tool.requested"}
      ]
    }
  ],
  "python_callback": "not_allowed"
}
